# Database Configuration - Use existing PostgreSQL container

# Database Configuration - H2 in-memory for tests
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Flyway - Disabled for tests
spring.flyway.enabled=false

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Cache
astro.encryption-key=I5iC/nmXIaTyg/6/MFT7uaslEmdKgLsahVVTIDxpNSg=
spring.cache.type=simple
# ------------------------------------------------------------
# Giá trị giả cho các biến môi trường mà application.properties yêu cầu.
#
# Không có khối này thì mọi test @SpringBootTest chỉ chạy được trên máy có sẵn
# file .env — mà .env bị gitignore, nên CI dựng context là hỏng ngay ở
# "Could not resolve placeholder 'MAIL_HOST'". Test không nên đòi bí mật thật:
# đây toàn giá trị vô hại, chỉ để context dựng lên được.
#
# Cố ý KHÔNG đặt mặc định trong application.properties: thiếu biến ở
# production thì phải hỏng to và hỏng sớm, chứ không âm thầm chạy bằng giá trị
# giả rồi gửi mail đi đâu không biết.
# ------------------------------------------------------------
spring.datasource.password=
jwt.secret=dGVzdC1vbmx5LXNlY3JldC1rZXktZm9yLWNpLW5vdC1hLXJlYWwtb25lLTEyMzQ1Ng==
spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.mail.host=localhost
spring.mail.port=2525
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
gemini.api.key=test-key
gemini.api.endpoint=http://localhost:0/test
